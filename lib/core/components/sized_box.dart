import 'package:flutter/material.dart';

/// a class for SizedBox
class DefaultSized extends StatelessWidget {
  const DefaultSized({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 10,
    );
  }
}
